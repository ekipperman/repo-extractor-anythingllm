{
  "name": "anythingllm-backend",
  "version": "1.0.0",
  "description": "Backend API for AnythingLLM project",
  "main": "src/server.js",
  "scripts": {
    "start": "NODE_ENV=production node src/server.js",     // ✅ Production flag
    "dev": "nodemon src/server.js"                         // ✅ Dev command stays
  },
  "dependencies": {
    "axios": "^1.6.2",                                     // ✅ Needed for external requests (AnythingLLM proxying)
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "pg": "^8.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
